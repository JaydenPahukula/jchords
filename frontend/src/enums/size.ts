/**
 * Radix theme breakpoints
 *
 * Values are `min-width` based and apply when the screen width is equal or greater.
 * - initial: `0px`
 * - xs: `520px`
 * - sm: `768px`
 * - md: `1024px`
 * - lg: `1280px`
 * - xl: `1640px`
 */
export const enum Size {
  initial,
  xs,
  sm,
  md,
  lg,
  xl,
}
